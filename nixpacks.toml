[phases.setup]
nixPkgs = ["python311"]

# Nixpacks instala automáticamente requirements.txt, solo necesitamos agregar Playwright
[phases.install]
cmds = [
  "playwright install chromium",
  "playwright install-deps chromium"
]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"

